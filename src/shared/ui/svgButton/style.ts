import styled from "styled-components"

export const Button = styled.button`
    display: grid;
    border: none;
    background: none;
    align-items: center;

    &:hover svg path{
        fill: #7a7979;
    }
`